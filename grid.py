class Grid:

    def __init__(
        self,
        grid,
        visibility_radius: int = 5,
    ):
        self.grid = grid
        self.prefix_sum = 0 #use prefix sum function
        self.visibility_radius = visibility_radius

    def get_num_robots_forward(position: tuple):
        return None
